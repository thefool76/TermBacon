import { ensureContractSchema, getWorkspaceId } from "@/lib/contract-store";
export async function GET(_request: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const workspaceId = await getWorkspaceId(); if (!workspaceId) return new Response("Not found", { status: 404 }); const env = await ensureContractSchema(); const row = await env.DB.prepare("SELECT file_key, file_name FROM contracts WHERE id = ? AND workspace_id = ? AND status = 'confirmed' LIMIT 1").bind(id, workspaceId).first<{ file_key: string; file_name: string }>(); if (!row) return new Response("Not found", { status: 404 }); const object = await env.CONTRACTS.get(row.file_key); if (!object) return new Response("Not found", { status: 404 }); return new Response(object.body, { headers: { "Content-Type": object.httpMetadata?.contentType || "application/pdf", "Content-Disposition": `inline; filename="${row.file_name.replace(/[\"\\]/g, "")}"`, "Cache-Control": "private, no-store" } }); }
